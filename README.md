# servico-django-docker
É necessário ter o Docker Compose instalado.
Precisa executá-lo com o comando "docker compose up --build" dentro da pasta docker-compose.yml
Se quiser parar os containers utilize "docker compose down"
